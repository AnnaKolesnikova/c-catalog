#include "menu/menu.h"
// #include "catalog/catalog.h"
#include "catalog/catalog.h"

#include <stdio.h>

// MENU_ITEM menuItems[10];

int main()
{
	// showMenu(menuItems, 10);
	showMenu();
	

	// CatItem *item = create("root");
	// Catalog cat = {item};

	// append_child(item, create("123"));
	// append_child(item->children[0], create("123q"));
	// append_child(item->children[0]->children[0], create("123qw"));
	// append_child(item, create("456"));
	// append_child(item, create("789"));
	// print_catalog(&cat);
	// print_children(item);	
	// print_children(item->children[0]);
	// read_file("catalog.txt");
	// printf("%s\n", get_name("--asdsad"));

	return 0;
}